'use strict';

/**
 * `is-admin` policy
 */

module.exports = (policyContext, config, { strapi }) => {
    // Add your own logic here.
    // const roles = policyContext.role
    // // strapi.log.info('In is-admin policy.');

    // // const canDoSomething = true;

    // if (roles = 'Administrator') {
    //   return true;
    // }

    return false;
};
